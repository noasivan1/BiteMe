sql related:

1- in MySQL you need to import both Schemas: user_management and G1_DB(the project DB)
2- after connecting Server (with G1_DB), click on import (once) for transferring users data from user_management 
   to G1_DB 

and now you can start using the system without any problems